[package]
name = "maud-exp"
version = "0.1.0"
edition = "2024"

[dependencies]
maud = { version = "0.27.0", features = ["rocket"] }
rocket = "0.5.1"
tokio = { version = "1.0.0", features = ["rt", "rt-multi-thread", "macros"] }
